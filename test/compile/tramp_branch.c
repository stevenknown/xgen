int *id;
int tk;
char *p;
int memcmp();
void next()
{
  while (tk = *p) {
      while (*p >= 'a')
        tk = p;
      while (id) {
        if (tk == id && memcmp()) { return; }
      }
  }
}
