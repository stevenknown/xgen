int N=100;
static int bug(int* restrict ptra, int* restrict ptrb, int* restrict ptrc, int n)
{

    int i;
    int j;
    int w;
    while(i < 16){
        while (j < N){
            w = 10;
        }
        //i++;
    }
}
