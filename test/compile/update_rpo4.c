int memcmp();
char * p;
int tk;
int * id;
void next()
{
  while (tk = *p) {
    while (p >= 'a')
      tk = p++;
    while (id) {
      if (tk == id && memcmp()) { return; }
    }
  }
}
