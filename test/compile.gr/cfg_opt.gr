region program @"program" () {
    var g:i32:(align(4));
    region func main () {
        truebr (eq $1, $2), L2;
        goto L1;  //goto is actually fallthrough to label L1.
                  //So it can be removed.
    	st g = add $1, $2;
        label L1;
        goto L1;
        label L2;
    	stpr $7 = xor $7, $3;
    };
}
